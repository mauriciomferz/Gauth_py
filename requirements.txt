# Core dependencies
requests>=2.31.0
pydantic>=2.0.0
cryptography>=41.0.0
python-jose[cryptography]>=3.3.0
fastapi>=0.100.0
uvicorn[standard]>=0.23.0

# Authentication and JWT
PyJWT>=2.8.0
passlib[bcrypt]>=1.7.4

# Networking and async
httpx>=0.24.0
aiohttp>=3.8.0

# Database (optional, similar to lib/pq in Go)
psycopg2-binary>=2.9.0
sqlalchemy>=2.0.0

# Redis (similar to go-redis)
redis>=4.6.0
aioredis>=2.0.0

# Utilities
click>=8.1.0
python-dotenv>=1.0.0
structlog>=23.1.0

# Monitoring and metrics (similar to Prometheus client)
prometheus-client>=0.17.0

# Policy engine (similar to OPA)
# Note: Python doesn't have a direct equivalent to OPA, but we can use 
# libraries like casbin or implement custom policy logic
casbin>=1.17.0

# UUID generation
uuid>=1.30

# Validation
marshmallow>=3.20.0

# Testing (in dev requirements, but included for reference)
pytest>=7.4.0
pytest-asyncio>=0.21.0